run = "bash warp_manager.sh"
entrypoint = "warp_manager.sh"

modules = ["bash"]

hidden = [".config"]

[nix]
channel = "stable-24_05"

[languages.bash]
pattern = "**/*.sh"
syntax = "bash"

[deployment]
run = ["bash", "warp_manager.sh"]
deploymentTarget = "cloudrun"
